language: rust
rust:
  - stable
  - nightly
install:
  - rustup component add rustfmt
  - rustup component add clippy
script:
  - cargo fmt -- --check
  # - cargo clippy -- -D warnings
  - cargo test
  - cargo package
jobs:
  allow_failures:
    - rust: nightly